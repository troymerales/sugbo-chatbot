# --- secrets: never commit ---
.env
.env.*
!.env.example
.streamlit/secrets.toml
*.pem
*-key.json
.mcp.json
.claude/settings.local.json

# --- private local notes ---
study/
chapters.txt
transcript.txt
tickets.csv

# The knowledge base itself is committed (config.DOCS_PATH) so every clone, CI
# run and deploy grounds on the same text. These two stay private:
docs/system-prompt.md
docs/homepage-reference.png

# --- backtest experiment: keep the sample + code, ignore real data + outputs ---
evaluation/data/jira_tickets*.csv
!evaluation/data/jira_tickets.sample.csv
evaluation/results/*
!evaluation/results/.gitkeep
# private local dashboard pointed at the real results (public repo ships the demo only)
evaluation/dashboard_real.py

# --- generated at runtime ---
.ipynb_checkpoints/
__pycache__/
*.egg-info/
.pytest_cache/
*.sqlite
*.db
soap_notes.db
logs/

error.png