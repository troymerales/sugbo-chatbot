# --- secrets: never commit ---
.env
.env.*
!.env.example
.streamlit/secrets.toml
*.pem
*-key.json                 # cloud service-account keys
# local Claude Code config (project-specific, may hold tokens)
.mcp.json
.claude/settings.local.json

# --- private study material (see study/README.md) ---
# Everything under study/ is personal learning notes + the cloud lab notebook.
# GitHub shows the professional project; study/ stays on the local machine only.
study/

chapters.txt
transcript.txt

# full knowledge base + hand-written prompt (private). The committed
# docs/SugboDoc-Documentation.sample.md excerpt is the fallback (config.DOCS_PATH).
docs/SugboDoc-Documentation.md
docs/system-prompt.md

# design reference screenshot (not part of the deliverable)
docs/homepage-reference.png

# generated at runtime
__pycache__/
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
*.sqlite
*.db
logs/
doc_proposals/
evaluation/eval_generated.jsonl
evaluation/eval_scorecard.json
tickets.csv

# local docker-compose overrides (machine-specific ports / mounts)
docker-compose.override.yml
