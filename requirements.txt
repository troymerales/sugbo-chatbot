# Streamlit runtime — streamlit_app.py + the assistant/ floating widget + core/.
# Streamlit Community Cloud installs exactly this file.
streamlit>=1.37
google-genai>=1.0
python-dotenv>=1.0
requests>=2.31

# Chat-log persistence to Supabase (core/chatlog.py -> api/db.py). Only used
# when DATABASE_URL is set; a lightweight install otherwise.
SQLAlchemy>=2.0
psycopg[binary]>=3.1

# Not here on purpose:
#   fastapi / uvicorn        -> requirements-service.txt   (the old REST backend)
#   numpy / scikit-learn     -> requirements-analytics.txt (batch failure clustering)
#   pytest / httpx           -> requirements-dev.txt
# The web widget (web/index.html) pulls marked.js + DOMPurify from a CDN — no pip.
